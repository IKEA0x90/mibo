# Allows to manage chat references.
# Shows the available references defined in ref.py as buttons
# When a reference is selected, enables a big text area to edit the content of the reference json.
# ref.py call must also save them back to the database and reload them in memory.